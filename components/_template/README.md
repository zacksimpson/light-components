# Template

One-line description: what the screen does and its key interaction (tap-to-select, long-press behavior, whatever's non-obvious).

---

## Depends on

* `com.thelightphone.sdk`: `SealedLightActivity`, `SimpleLightScreen`
* `com.thelightphone.sdk.ui`: `LightTheme`, `LightThemeController`, `LightThemeTokens`, `LightText`, `LightTextVariant`
* anything else it pulls in, third-party libraries included

## Pasting this in

1. Rename the `package` declaration and the `TemplateScreen` class/file name.
2. Note the return type if `SimpleLightScreen<Unit>` isn't right for this screen, and what a caller gets back.

> [!NOTE]
> Delete this file's placeholder body and this note once real content replaces it.
